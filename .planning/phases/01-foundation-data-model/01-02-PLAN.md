---
phase: 01-foundation-data-model
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/collections/Categories.ts
  - src/collections/Products.ts
  - src/seed.ts
  - payload.config.ts
autonomous: true
requirements:
  - ADM-01
  - ADM-02
  - ADM-05
  - ADM-06
  - INF-02
  - INF-03

must_haves:
  truths:
    - "Admin can create a product with name, description, category, and multiple images"
    - "Admin can reorder product images via drag-and-drop in admin panel"
    - "Admin can create, edit, and delete categories"
    - "Unauthenticated users cannot create, update, or delete products or categories"
    - "REST API at /api/products and /api/categories returns data publicly"
    - "An initial admin user exists for login"
  artifacts:
    - path: "src/collections/Products.ts"
      provides: "Products collection with name, slug, description, images, category"
      exports: ["Products"]
    - path: "src/collections/Categories.ts"
      provides: "Categories collection with name, slug, description, image, sortOrder"
      exports: ["Categories"]
    - path: "src/seed.ts"
      provides: "Admin user seed script"
      contains: "payload.create"
  key_links:
    - from: "src/collections/Products.ts"
      to: "src/collections/Media.ts"
      via: "upload relationship (images field)"
      pattern: "relationTo.*media"
    - from: "src/collections/Products.ts"
      to: "src/collections/Categories.ts"
      via: "relationship field (category)"
      pattern: "relationTo.*categories"
    - from: "payload.config.ts"
      to: "src/collections/Products.ts"
      via: "collections array"
      pattern: "import.*Products"
    - from: "payload.config.ts"
      to: "src/seed.ts"
      via: "onInit hook"
      pattern: "onInit"
---

<objective>
Create the Products and Categories collections with full field definitions, access control, and slug auto-generation. Seed an initial admin user so the admin panel is usable.

Purpose: Complete the data model so admin can manage the entire product catalog. This plan implements the core CRUD capability that Phase 1 exists to deliver.
Output: Working Products and Categories collections with admin CRUD, public API read access, and a seeded admin account.
</objective>

<execution_context>
@C:/Users/ricar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-model/01-RESEARCH.md
@.planning/phases/01-foundation-data-model/01-VALIDATION.md
@.planning/phases/01-foundation-data-model/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor needs these contracts -->

From src/collections/Media.ts:
```typescript
import type { CollectionConfig } from 'payload'
export const Media: CollectionConfig  // slug: 'media'
// Fields: alt (text, required), blurDataURL (text, hidden)
// Upload with imageSizes: thumbnail, card, detail, zoom
```

From src/lib/slugFormat.ts:
```typescript
export const formatSlug = (val: string): string
// Strips Portuguese diacritics, lowercases, replaces non-alphanumeric with hyphens
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Categories and Products collections with access control and slug hooks</name>
  <files>src/collections/Categories.ts, src/collections/Products.ts, payload.config.ts</files>
  <action>
    Create `src/collections/Categories.ts`:
    - slug: 'categories'
    - admin.useAsTitle: 'name'
    - Access: read public, create/update/delete admin-only (use `isAdmin` pattern: `({ req }) => Boolean(req.user)`)
    - Fields:
      - name (text, required)
      - slug (text, unique, sidebar position, beforeValidate hook using formatSlug from @/lib/slugFormat -- generate from name on create if slug is empty)
      - description (textarea, optional)
      - image (upload, relationTo: 'media', optional -- category cover image)
      - sortOrder (number, defaultValue: 0, sidebar position)

    Create `src/collections/Products.ts`:
    - slug: 'products'
    - admin.useAsTitle: 'name', defaultColumns: ['name', 'category', 'createdAt']
    - Access: read public, create/update/delete admin-only
    - Fields:
      - name (text, required)
      - slug (text, unique, sidebar position, beforeValidate hook using formatSlug -- generate from name on create if slug is empty)
      - description (richText -- uses Lexical editor configured in payload.config.ts)
      - images (upload, relationTo: 'media', hasMany: true, required: true, admin.isSortable: true -- enables drag-and-drop reordering)
      - category (relationship, relationTo: 'categories', required: true)
      - sortOrder (number, defaultValue: 0, sidebar position)
    - Hooks:
      - afterChange: Add a commented-out revalidatePath stub for Phase 2 (e.g., `// revalidatePath('/products/' + doc.slug)`)

    Update `payload.config.ts`:
    - Import Categories and Products
    - Add to collections array: [Media, Categories, Products] (Media should already be there from Plan 01)
    - Ensure Users collection is included (Payload includes it by default, but verify)

    IMPORTANT: The isAdmin access function should be defined once and reused. Either define it in each collection file or create a shared `src/lib/access.ts` utility. Prefer shared utility to avoid duplication.
  </action>
  <verify>
    <automated>cd D:/personal/projects/rmjewelery && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Categories collection visible in admin sidebar with all fields
    - Products collection visible in admin sidebar with name, slug, description, images (sortable), category relationship
    - Slug auto-generates from name on product/category creation
    - GET /api/products and GET /api/categories return 200 (empty arrays)
    - POST /api/products without auth returns 401/403
  </done>
</task>

<task type="auto">
  <name>Task 2: Create admin seed script and wire into Payload onInit</name>
  <files>src/seed.ts, payload.config.ts</files>
  <action>
    Create `src/seed.ts` that:
    1. Accepts a Payload instance as argument
    2. Checks if any users exist: `const existing = await payload.find({ collection: 'users', limit: 1 })`
    3. If no users exist, creates the initial admin:
       ```typescript
       await payload.create({
         collection: 'users',
         data: {
           email: 'admin@rmjewelery.com',
           password: 'changeme123',
         },
       })
       ```
    4. Logs a message: "Seeded initial admin user: admin@rmjewelery.com"
    5. If users already exist, logs: "Admin user already exists, skipping seed"

    Update `payload.config.ts` to add an `onInit` hook:
    ```typescript
    onInit: async (payload) => {
      const { seed } = await import('./seed')
      await seed(payload)
    },
    ```

    IMPORTANT: The seed password 'changeme123' is for development only. Add a comment reminding to change it. In production, the admin should change the password immediately via the admin panel.

    After wiring the seed, run `npm run dev` briefly to trigger onInit and verify the admin user is created. Then verify login works at /admin by checking the seed log output.
  </action>
  <verify>
    <automated>cd D:/personal/projects/rmjewelery && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Dev server startup creates admin user automatically (first run only)
    - Admin can log in at /admin with admin@rmjewelery.com / changeme123
    - Subsequent starts skip seeding (no duplicate user errors)
    - Admin can create categories and products through the admin panel
  </done>
</task>

</tasks>

<verification>
1. Build passes: `npx next build`
2. Dev server starts, admin user seeded on first run
3. Admin login works at /admin
4. Can create a category, then create a product referencing that category with multiple images
5. GET /api/products returns the created product publicly
6. POST /api/products without auth token is rejected
</verification>

<success_criteria>
- Products and Categories collections fully functional in admin panel
- Product images are sortable via drag-and-drop
- Slugs auto-generate with Portuguese diacritics stripped
- Access control enforced: public read, admin-only write
- REST API serves collection data at /api/products and /api/categories
- Initial admin user seeded on first startup
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-model/01-02-SUMMARY.md`
</output>
