---
phase: 01-foundation-data-model
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - tests/helpers/payload.ts
  - tests/collections/products.test.ts
  - tests/collections/categories.test.ts
  - tests/collections/media.test.ts
  - tests/access-control.test.ts
  - tests/api.test.ts
  - vitest.config.ts
  - package.json
autonomous: false
requirements:
  - ADM-01
  - ADM-02
  - ADM-05
  - ADM-06
  - INF-01
  - INF-02
  - INF-03

must_haves:
  truths:
    - "All Phase 1 requirements have automated test coverage"
    - "Tests prove products CRUD works via Payload Local API"
    - "Tests prove categories CRUD works via Payload Local API"
    - "Tests prove image upload generates multiple sizes"
    - "Tests prove access control blocks unauthenticated writes"
    - "Admin can visually confirm full workflow in browser"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration for Payload integration tests"
      contains: "defineConfig"
    - path: "tests/helpers/payload.ts"
      provides: "Shared test helper initializing Payload with test database"
      exports: ["getTestPayload"]
    - path: "tests/collections/products.test.ts"
      provides: "Products collection integration tests"
      contains: "describe"
    - path: "tests/collections/categories.test.ts"
      provides: "Categories collection integration tests"
      contains: "describe"
    - path: "tests/access-control.test.ts"
      provides: "Access control verification tests"
      contains: "describe"
  key_links:
    - from: "tests/helpers/payload.ts"
      to: "payload.config.ts"
      via: "imports Payload config for test initialization"
      pattern: "import.*payload.config"
    - from: "tests/collections/products.test.ts"
      to: "tests/helpers/payload.ts"
      via: "uses shared test helper"
      pattern: "import.*getTestPayload"
---

<objective>
Create integration tests validating all Phase 1 requirements and perform a human verification of the complete admin workflow.

Purpose: Establish the test infrastructure (Wave 0 from VALIDATION.md) and prove every Phase 1 requirement works before moving to Phase 2. Tests become the regression safety net for all future phases.
Output: Full Vitest integration test suite covering products, categories, media, and access control. Human-verified admin workflow.
</objective>

<execution_context>
@C:/Users/ricar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-model/01-RESEARCH.md
@.planning/phases/01-foundation-data-model/01-VALIDATION.md
@.planning/phases/01-foundation-data-model/01-01-SUMMARY.md
@.planning/phases/01-foundation-data-model/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 and Plan 02 outputs -->

Collections available (all registered in payload.config.ts):
- 'media' — upload collection with imageSizes (thumbnail, card, detail, zoom), blurDataURL field
- 'categories' — name, slug, description, image, sortOrder
- 'products' — name, slug, description (richText), images (hasMany upload to media, sortable), category (relationship to categories), sortOrder
- 'users' — Payload built-in, email/password auth

Access control pattern:
- read: () => true (all collections)
- create/update/delete: ({ req }) => Boolean(req.user) (all collections)

API endpoints:
- GET/POST /api/products
- GET/POST /api/categories
- GET/POST /api/media
- POST /api/users/login
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Vitest and create integration tests for all Phase 1 requirements</name>
  <files>
    vitest.config.ts, package.json,
    tests/helpers/payload.ts,
    tests/collections/products.test.ts,
    tests/collections/categories.test.ts,
    tests/collections/media.test.ts,
    tests/access-control.test.ts
  </files>
  <action>
    1. Install Vitest: `npm install -D vitest`

    2. Create `vitest.config.ts`:
       - Use defineConfig from vitest/config
       - Set test.globals: true, test.testTimeout: 30000 (integration tests need time)
       - Set resolve.alias for '@' to './src' to match Next.js path aliases

    3. Create `tests/helpers/payload.ts`:
       - Export a `getTestPayload()` function that initializes Payload using the Local API
       - Use a TEST_DATABASE_URI env var (fallback to DATABASE_URI) so tests can use a separate database
       - Import the payload config and initialize with `payload.init()` or `getPayload()`
       - Cache the instance so multiple test files share one initialization
       - NOTE: Payload 3 initialization for testing may require importing from 'payload' directly and passing the config. Check the actual Payload 3 API -- use `import { getPayload } from 'payload'` and pass the config object.

    4. Create `tests/collections/categories.test.ts` (ADM-06):
       - Test: "can create a category with name and slug auto-generates"
       - Test: "can update a category name"
       - Test: "can delete a category"
       - Test: "slug strips Portuguese diacritics" (create category named "Colecao Verao", expect slug "colecao-verao")

    5. Create `tests/collections/products.test.ts` (ADM-01, ADM-02, ADM-05):
       - Setup: Create a test category and upload a test media item first
       - Test: "can create a product with name, description, and category" (ADM-01)
       - Test: "can upload multiple images to a product" (ADM-02) -- create product with array of media IDs
       - Test: "image order is preserved when creating product" (ADM-05) -- create with [mediaId1, mediaId2], read back, verify order
       - Test: "slug auto-generates from product name"
       - For test images: create a minimal 1x1 PNG buffer in code (Buffer.from for a tiny valid PNG) and upload via payload.create on the media collection

    6. Create `tests/collections/media.test.ts` (INF-01):
       - Test: "uploaded image generates thumbnail, card, detail, zoom sizes" -- upload a test image, verify doc.sizes has all four keys
       - Test: "blurDataURL is generated after upload" -- upload image, re-fetch, check blurDataURL starts with 'data:image'
       - For test image: use a programmatically created small JPEG/PNG buffer (e.g., using Sharp to create a 100x100 solid color image, or a hardcoded minimal valid JPEG buffer)

    7. Create `tests/access-control.test.ts` (INF-02):
       - Test: "unauthenticated cannot create products" (INF-02) -- use `overrideAccess: false` with no user, expect error
       - Test: "unauthenticated cannot delete products" (INF-02)
       - Test: "authenticated admin can create products" -- pass user in req
       - NOTE: Payload Local API by default overrides access. Use `overrideAccess: false` to test actual access control. For authenticated calls, pass `user` in the options.

    8. Create `tests/api.test.ts` (INF-03):
       - Test: "unauthenticated can read products" (INF-03) -- use `overrideAccess: false` with no user, verify returns data
       - Test: "unauthenticated can read categories" (INF-03) -- use `overrideAccess: false` with no user
       - Test: "REST API returns product data with category relationship populated"

    IMPORTANT: Payload 3 test initialization may differ from Payload 2. The key is to call `getPayload({ config })` passing the imported config. If this doesn't work during execution, adapt to the actual Payload 3 testing API. The executor should check Payload 3 docs if initialization fails.

    IMPORTANT: For media upload tests, the test needs to provide a file. In Payload Local API, creating a media doc with a file requires passing `file` in the create options: `payload.create({ collection: 'media', data: { alt: 'test' }, file: { data: buffer, mimetype: 'image/png', name: 'test.png', size: buffer.length } })`.
  </action>
  <verify>
    <automated>cd D:/personal/projects/rmjewelery && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All tests pass: categories CRUD, products CRUD with images, media processing, access control
    - Test infrastructure reusable for future phases
    - Every Phase 1 requirement ID has at least one test
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 1 admin workflow</name>
  <action>
    Present the verification checklist to the user. The dev server should already be running or can be started with `npm run dev`.
    What was built: Complete Phase 1 -- Payload CMS with Products, Categories, and Media collections,
    admin panel at /admin with full CRUD, image processing pipeline (4 WebP sizes + blur placeholder),
    access-controlled REST API, and integration test suite.
  </action>
  <verify>
    Human confirms all 8 verification steps pass:
    1. Start dev server: `npm run dev`
    2. Navigate to http://localhost:3000/admin
    3. Log in with admin@rmjewelery.com / changeme123
    4. Create a category (e.g., "Aneis" or "Colares")
    5. Upload a jewelry photo as a product -- verify multiple image sizes generated (check /media folder for thumbnail/card/detail/zoom variants)
    6. Create a second product with multiple images -- drag to reorder images, save, reload, verify order persisted
    7. Open a new incognito tab and visit http://localhost:3000/api/products -- verify JSON response with your products
    8. Try POST to http://localhost:3000/api/products in incognito (no auth) -- should be rejected
  </verify>
  <done>User types "approved" confirming the admin workflow works end-to-end</done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all tests green
2. Human confirms admin login, CRUD, image processing, and API access control
</verification>

<success_criteria>
- Integration test suite covers all 7 Phase 1 requirement IDs
- All tests pass in under 30 seconds
- Human confirms admin can manage products, categories, and images end-to-end
- Human confirms image processing generates WebP variants
- Human confirms API is publicly readable but write-protected
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-model/01-03-SUMMARY.md`
</output>
