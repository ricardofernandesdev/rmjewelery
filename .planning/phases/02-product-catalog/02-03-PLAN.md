---
phase: 02-product-catalog
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - tests/components/instagram-cta.test.ts
  - tests/components/header.test.ts
  - tests/pages/catalog.test.ts
  - tests/pages/product-detail.test.ts
  - tests/pages/category.test.ts
autonomous: false
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - CONT-01
  - CONT-02
  - HOME-03
  - HOME-04

must_haves:
  truths:
    - "All Phase 2 test files exist and pass"
    - "Instagram CTA links to ig.me/m/{username}"
    - "Navigation has max 5-6 items"
    - "Site is visually responsive on mobile and desktop"
    - "Instagram DM button is visually accessible and prominent"
  artifacts:
    - path: "tests/components/instagram-cta.test.ts"
      provides: "Unit tests for Instagram CTA href format and content"
    - path: "tests/components/header.test.ts"
      provides: "Unit tests for navigation item count"
    - path: "tests/pages/catalog.test.ts"
      provides: "Integration test for catalog page query"
    - path: "tests/pages/product-detail.test.ts"
      provides: "Integration test for product detail page query"
    - path: "tests/pages/category.test.ts"
      provides: "Integration test for category filtering query"
  key_links:
    - from: "tests/components/instagram-cta.test.ts"
      to: "src/components/product/InstagramCTA.tsx"
      via: "import and render test"
      pattern: "InstagramCTA"
    - from: "tests/pages/catalog.test.ts"
      to: "src/lib/queries.ts"
      via: "query helper test"
      pattern: "getAllProducts"
---

<objective>
Write automated tests covering all Phase 2 requirements and perform human visual verification of the responsive layout and Instagram CTA.

Purpose: Validates that the catalog browsing experience works correctly and looks good on all devices.
Output: Passing test suite + human sign-off on visual quality and responsiveness.
</objective>

<execution_context>
@C:/Users/ricar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-product-catalog/02-RESEARCH.md
@.planning/phases/02-product-catalog/02-01-SUMMARY.md
@.planning/phases/02-product-catalog/02-02-SUMMARY.md
@.planning/phases/02-product-catalog/02-VALIDATION.md

@src/components/product/InstagramCTA.tsx
@src/components/layout/Header.tsx
@src/lib/queries.ts
@vitest.config.ts
@tests/helpers/payload.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write automated tests for Phase 2 components and pages</name>
  <files>tests/components/instagram-cta.test.ts, tests/components/header.test.ts, tests/pages/catalog.test.ts, tests/pages/product-detail.test.ts, tests/pages/category.test.ts</files>
  <behavior>
    - InstagramCTA: renders anchor with href matching `https://ig.me/m/{username}`, has target="_blank", contains "Estou interessado" text, shows @username fallback
    - Header nav: renders max 6 nav items (Inicio, Catalogo, up to 4 categories)
    - Catalog query: getAllProducts returns array with expected shape (name, slug, images, category)
    - Product detail query: getProductBySlug returns product with populated images and category at depth 2
    - Category query: getProductsByCategory returns filtered products belonging to specified category
  </behavior>
  <action>
1. Create `tests/components/instagram-cta.test.ts`:
   - Test that InstagramCTA renders an anchor element
   - Test href equals `https://ig.me/m/rmjewelery` (default username)
   - Test target="_blank" and rel="noopener noreferrer" attributes
   - Test "Estou interessado" text content
   - Test @username fallback text is present
   - Note: Since this is a Server Component, test by importing and calling it as an async function, then inspecting the JSX output. Alternatively, test the logic (URL construction) in isolation if direct rendering is not feasible with Vitest. Use a pragmatic approach — if Server Component rendering is difficult, extract the URL logic into a testable helper.

2. Create `tests/components/header.test.ts`:
   - Test that navigation structure enforces max 6 items
   - Can test by verifying the logic: given N categories, header should render min(N, 4) category links + 2 fixed links (Inicio, Catalogo)

3. Create `tests/pages/catalog.test.ts` — integration test using Payload Local API:
   - Use the existing test helper from tests/helpers/payload.ts
   - Create a test category and 2-3 test products
   - Call getAllProducts() and verify results include created products
   - Verify products have populated images (typeof object, not string)
   - Verify products have populated category (typeof object, not string)

4. Create `tests/pages/product-detail.test.ts` — integration test:
   - Create a test product with known slug
   - Call getProductBySlug(slug) and verify it returns the product
   - Verify product has name, slug, images array, category object
   - Call getProductBySlug('nonexistent') and verify it returns null

5. Create `tests/pages/category.test.ts` — integration test:
   - Create 2 categories and products in each
   - Call getProductsByCategory(categorySlug) for one category
   - Verify only products from that category are returned
   - Call getCategoryBySlug(slug) and verify it returns category

Run all tests: `npx vitest run --reporter=verbose`
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&amp;1 | tail -30</automated>
  </verify>
  <done>All Phase 2 test files exist and all tests pass. Coverage: InstagramCTA URL format (CONT-01, CONT-02), nav item count (HOME-03), catalog queries (CAT-01), product detail queries (CAT-02), category filtering (CAT-03).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of responsive catalog and Instagram CTA</name>
  <action>
Human verifies the complete product catalog experience built in Plans 01 and 02.

What was built: Complete product catalog with responsive layout, category browsing, product detail pages with image gallery, and Instagram DM contact button.

How to verify:
1. Start dev server: `npm run dev`
2. Visit http://localhost:3000 — verify header with logo, nav links, and footer with Instagram link render
3. Visit http://localhost:3000/products — verify product grid displays (if products exist in admin)
   - If no products: go to /admin, log in, create a category and 2-3 products with images first
4. Check responsive layout: resize browser to mobile width — verify 2-column grid, hamburger menu appears
5. Tap hamburger menu — verify mobile nav opens with all links, closes on tap
6. Click a product card — verify product detail page shows: image gallery, name, description, Instagram CTA
7. Click a category in nav or filter pills — verify category page shows only products from that category
8. Click "Estou interessado" button — verify it opens Instagram DM link (ig.me/m/{username})
9. Verify overall visual quality: clean luxury aesthetic, proper spacing, elegant typography

Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>Human approval</verify>
  <done>Human confirms: responsive layout works on mobile and desktop, product browsing flow is complete, Instagram CTA functions correctly, visual quality meets luxury brand standard.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — all tests pass (existing Phase 1 + new Phase 2 tests)
- Human verifies visual quality and responsiveness
- Human verifies Instagram CTA works
</verification>

<success_criteria>
- All 5 new test files pass
- Full test suite (Phase 1 + Phase 2) is green
- Human approves visual layout on mobile and desktop
- Human confirms Instagram DM button functions correctly
- All 7 Phase 2 requirement IDs covered by tests or visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-catalog/02-03-SUMMARY.md`
</output>
