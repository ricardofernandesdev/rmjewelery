---
phase: 02-product-catalog
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/(frontend)/products/page.tsx
  - src/app/(frontend)/products/[slug]/page.tsx
  - src/app/(frontend)/categories/[slug]/page.tsx
  - src/components/product/ProductGrid.tsx
  - src/components/product/ProductCard.tsx
  - src/components/product/ProductGallery.tsx
  - src/components/product/InstagramCTA.tsx
  - src/collections/Products.ts
  - src/collections/Categories.ts
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - CONT-01
  - CONT-02

must_haves:
  truths:
    - "Visitor sees products in a photo-forward grid (2 cols mobile, 3 md, 4 lg)"
    - "Visitor can tap a category to see only products in that category"
    - "Visitor can open a product detail page showing name, description, and multiple photo angles"
    - "Visitor can tap 'Estou interessado' and be redirected to Instagram DMs"
    - "Instagram CTA shows @username fallback text for desktop edge cases"
    - "Admin product changes are reflected on public site via revalidation"
  artifacts:
    - path: "src/app/(frontend)/products/page.tsx"
      provides: "Catalog page with product grid"
    - path: "src/app/(frontend)/products/[slug]/page.tsx"
      provides: "Product detail page with gallery and Instagram CTA"
    - path: "src/app/(frontend)/categories/[slug]/page.tsx"
      provides: "Category page filtering products"
    - path: "src/components/product/ProductCard.tsx"
      provides: "Product card with image, name, category"
    - path: "src/components/product/ProductGallery.tsx"
      provides: "Client Component image gallery with thumbnail selection"
    - path: "src/components/product/InstagramCTA.tsx"
      provides: "Instagram DM button with inline SVG icon"
  key_links:
    - from: "src/app/(frontend)/products/page.tsx"
      to: "src/lib/queries.ts"
      via: "getAllProducts"
      pattern: "getAllProducts"
    - from: "src/app/(frontend)/products/[slug]/page.tsx"
      to: "src/lib/queries.ts"
      via: "getProductBySlug"
      pattern: "getProductBySlug"
    - from: "src/app/(frontend)/categories/[slug]/page.tsx"
      to: "src/lib/queries.ts"
      via: "getCategoryBySlug and getProductsByCategory"
      pattern: "getProductsByCategory"
    - from: "src/components/product/InstagramCTA.tsx"
      to: "Instagram DM"
      via: "ig.me/m/{username} link"
      pattern: "ig\\.me/m/"
    - from: "src/collections/Products.ts"
      to: "next/cache"
      via: "revalidatePath in afterChange"
      pattern: "revalidatePath"
---

<objective>
Build the catalog browsing experience: product grid page, category filtering page, product detail page with image gallery, and Instagram DM contact button. Implement revalidation hooks so admin changes appear on the public site.

Purpose: Delivers the core user journey — browse products, view details, express interest via Instagram.
Output: Three page routes (/products, /products/[slug], /categories/[slug]) with all supporting components.
</objective>

<execution_context>
@C:/Users/ricar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ricar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-product-catalog/02-RESEARCH.md
@.planning/phases/02-product-catalog/02-01-SUMMARY.md

@src/lib/queries.ts
@src/lib/payload.ts
@src/collections/Products.ts
@src/collections/Categories.ts
@src/collections/Media.ts
@src/app/(frontend)/layout.tsx
@src/components/ui/Container.tsx

<interfaces>
<!-- From Plan 01 src/lib/queries.ts (will exist after Plan 01) -->
```typescript
export async function getAllProducts(limit?: number): Promise<PaginatedDocs<Product>>
export async function getProductBySlug(slug: string): Promise<Product | null>
export async function getProductsByCategory(categorySlug: string, limit?: number): Promise<PaginatedDocs<Product>>
export async function getAllCategories(): Promise<PaginatedDocs<Category>>
export async function getCategoryBySlug(slug: string): Promise<Category | null>
```

<!-- Media sizes available for images -->
Media sizes: thumbnail (400x400), card (800x800), detail (1600w), zoom (2400w)
Media fields: url, alt, blurDataURL, sizes.{name}.url/width/height

<!-- Product images field is hasMany upload to media — returns array of Media objects at depth 1+ -->
<!-- Product category field is relationship to categories — returns Category object at depth 1+ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create product components and catalog/category pages</name>
  <files>src/components/product/ProductCard.tsx, src/components/product/ProductGrid.tsx, src/app/(frontend)/products/page.tsx, src/app/(frontend)/categories/[slug]/page.tsx</files>
  <action>
1. Create `src/components/product/ProductCard.tsx` — Server Component:
   - Accepts product (with populated images and category at depth 1)
   - Renders a Link to `/products/${product.slug}` wrapping:
     - Aspect-square container with overflow-hidden and rounded-sm
     - First image from product.images array using Next.js Image component
     - Use `sizes.card.url` (800x800) for the image source, fall back to `url`
     - Set `placeholder="blur"` with `blurDataURL` if available
     - Subtle scale-105 transition on group-hover
   - Below image: product name (text-sm font-medium), category name (text-xs text-brand-gray)
   - Handle edge cases: no images (show brand-cream placeholder div), unpopulated category (typeof check)

2. Create `src/components/product/ProductGrid.tsx` — Server Component:
   - Accepts products array and optional emptyMessage string
   - Grid layout: `grid grid-cols-2 gap-3 sm:gap-4 md:grid-cols-3 lg:grid-cols-4`
   - Maps products to ProductCard components
   - Shows emptyMessage or "Nenhum produto encontrado." if array is empty

3. Create `src/app/(frontend)/products/page.tsx` — Server Component:
   - Fetch all products via `getAllProducts()`
   - Also fetch all categories via `getAllCategories()` for filter pills/tabs at top
   - Render page title "Catalogo" in a Container
   - Render category filter pills: horizontal scrollable row of links to `/categories/{slug}`, with an "Todos" active pill linking to `/products`
   - Render ProductGrid with fetched products
   - Add `generateMetadata` returning `{ title: 'Catalogo | RM Jewelry' }`

4. Create `src/app/(frontend)/categories/[slug]/page.tsx` — Server Component:
   - IMPORTANT: Next.js 15 params are Promises — use `{ params }: { params: Promise<{ slug: string }> }` and `await params`
   - Fetch category via `getCategoryBySlug(slug)`, call `notFound()` if null
   - Fetch products via `getProductsByCategory(slug)`
   - Also fetch all categories for the same filter pills row (current category highlighted)
   - Render category name as page title, then ProductGrid
   - Add `generateMetadata` with category name
   - Add `generateStaticParams` fetching all category slugs
  </action>
  <verify>
    <automated>npx next build 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Catalog page renders product grid at /products. Category pages filter by category at /categories/[slug]. Category pills show on both pages. Build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create product detail page with gallery, Instagram CTA, and revalidation hooks</name>
  <files>src/components/product/ProductGallery.tsx, src/components/product/InstagramCTA.tsx, src/app/(frontend)/products/[slug]/page.tsx, src/collections/Products.ts, src/collections/Categories.ts</files>
  <action>
1. Create `src/components/product/InstagramCTA.tsx` — Server Component:
   - Read username from `process.env.NEXT_PUBLIC_INSTAGRAM_USERNAME || 'rmjewelery'`
   - Render an anchor tag to `https://ig.me/m/${username}`
   - Attributes: `target="_blank" rel="noopener noreferrer"`
   - Styling: `inline-flex items-center gap-2 bg-brand-dark text-white px-6 py-3 rounded-full hover:bg-brand-gray transition-colors text-base`
   - Inline SVG Instagram icon (24x24 viewBox, standard Instagram glyph path — use the exact SVG from research Pattern 5)
   - Text: "Estou interessado"
   - Visible fallback: `<span className="text-sm opacity-75">@{username}</span>` for desktop edge cases where ig.me may not open the app
   - Minimum 44px touch target height (py-3 achieves this)

2. Create `src/components/product/ProductGallery.tsx` — Client Component (`'use client'`):
   - Accepts images array (populated Media objects with sizes)
   - State: selectedIndex (default 0)
   - Main image: large display using `sizes.detail.url` (1600w), with `placeholder="blur"` and blurDataURL
   - Thumbnail strip below: horizontal row of thumbnail images (sizes.thumbnail.url, 400x400)
   - Click thumbnail to change selectedIndex
   - Main image uses `aspect-[3/4]` ratio (portrait, good for jewelry)
   - On mobile: thumbnails scroll horizontally if many images
   - Use Next.js Image component for all images

3. Create `src/app/(frontend)/products/[slug]/page.tsx` — Server Component:
   - IMPORTANT: Next.js 15 params are Promises — use `{ params }: { params: Promise<{ slug: string }> }` and `await params`
   - Fetch product via `getProductBySlug(slug)`, call `notFound()` if null
   - Layout: two-column on desktop (gallery left, info right), single column on mobile
   - Left: ProductGallery with product.images
   - Right: product name (heading, serif font), category name with link to `/categories/{slug}`, description rendered via `RichText` from `@payloadcms/richtext-lexical/react`, then InstagramCTA button
   - Add `generateMetadata` with product name and first image as OG image (basic, Phase 5 will enhance)
   - Add `generateStaticParams` fetching all product slugs
   - Import RichText: `import { RichText } from '@payloadcms/richtext-lexical/react'`
   - Render description: `{product.description && <RichText data={product.description} />}`

4. Update `src/collections/Products.ts` — replace TODO afterChange hook with real revalidation:
   ```typescript
   import { revalidatePath } from 'next/cache'
   // In afterChange hook:
   afterChange: [
     ({ doc }) => {
       revalidatePath(`/products/${doc.slug}`)
       revalidatePath('/products')
       revalidatePath('/')
       if (typeof doc.category === 'object' && doc.category?.slug) {
         revalidatePath(`/categories/${doc.category.slug}`)
       }
       return doc
     },
   ],
   ```

5. Update `src/collections/Categories.ts` — add afterChange revalidation hook:
   ```typescript
   import { revalidatePath } from 'next/cache'
   // Add hooks.afterChange:
   afterChange: [
     ({ doc }) => {
       revalidatePath(`/categories/${doc.slug}`)
       revalidatePath('/products')
       revalidatePath('/')
       return doc
     },
   ],
   ```
  </action>
  <verify>
    <automated>npx next build 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Product detail page renders at /products/[slug] with image gallery, rich text description, and Instagram CTA button. CTA links to ig.me/m/{username} with inline SVG icon and @username fallback. Revalidation hooks on Products and Categories collections. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- /products route renders ProductGrid
- /products/[slug] route renders product detail with gallery and InstagramCTA
- /categories/[slug] route renders filtered products
- InstagramCTA href matches `https://ig.me/m/{username}` pattern
- ProductGallery.tsx has 'use client' directive
- Products.ts afterChange calls revalidatePath (no more TODO comment)
- Categories.ts has afterChange with revalidatePath
</verification>

<success_criteria>
- Product grid shows photo-forward cards with blur placeholders
- Category filtering works via /categories/[slug]
- Product detail shows multiple images, description, and Instagram CTA
- "Estou interessado" button links to ig.me/m/{username} with Instagram icon
- @username visible as fallback text
- Admin changes trigger revalidation of affected pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-catalog/02-02-SUMMARY.md`
</output>
